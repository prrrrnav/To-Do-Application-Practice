// import styles from '../style.module.css'

const Form = () => {
    return (
        <div>
            <Form>
                <input type="text" placeholder="Add todo item" />
                <button>Add</button>
            </Form>
        </div>
    );
};

export default Form;